# Isolated runtime data, tokens, and history (CRITICAL: NEVER COMMIT CREDENTIALS)
data/
jetski-standalone-oauth-token
*.token
*.db
*.db-shm
*.db-wal
*.log

# Large core binaries (~193 MB Google proprietary CLI engines)
Windows/bin/agy.exe
Windows/bin/*.old
macOS/bin/agy
Linux/bin/agy

# OS / Editor artifacts
.vs/
.vscode/
Thumbs.db
Desktop.ini
.DS_Store

# Local release build artifacts (releases are hosted directly on GitHub Releases)
Release/
*.zip
